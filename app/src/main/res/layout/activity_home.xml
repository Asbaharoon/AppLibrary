<?xml version="1.0" encoding="utf-8"?>
<FrameLayout android:id="@+id/container_Fragments"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <android.support.v4.widget.DrawerLayout

        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/drawer-layout"
        tools:context="com.semicolon.librarians.libraryguide.Activities.HomeActivity">

        <include layout="@layout/home_content" android:id="@+id/HomeContent"/>
        <android.support.design.widget.NavigationView
            android:id="@+id/navView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            app:headerLayout="@layout/drawer_header"
            app:itemTextColor="@color/toolbar_homepage"
            app:itemIconTint="@color/toolbar_homepage"
            app:menu="@menu/menu"
            >

        </android.support.design.widget.NavigationView>
        <!--<com.rom4ek.arcnavigationview.ArcNavigationView
            android:id="@+id/arcDrawer"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:fitsSystemWindows="true"
            android:layout_gravity="start"
            android:background="@color/base"
            app:itemBackground="@color/base"
            app:headerLayout="@layout/drawer_header"
            app:itemTextColor="@color/toolbar_homepage"
            app:itemIconTint="@color/toolbar_homepage"
            app:menu="@menu/menu"
            >

        </com.rom4ek.arcnavigationview.ArcNavigationView>-->

    </android.support.v4.widget.DrawerLayout>
</FrameLayout>

